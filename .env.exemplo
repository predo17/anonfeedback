VITE_SUPABASE_URL
VITE_SUPABASE_ANON_KEY

Siga os passos abaixo:

Acesse o Dashboard do Supabase
👉 https://supabase.com/dashboard

Selecione o projeto desejado

No menu lateral, vá em:
Project Settings → API

Em Project URL, copie o valor e use em:

VITE_SUPABASE_URL=https://seu-projeto-id.supabase.co

Em Project API keys, copie a chave anon public e use em:

VITE_SUPABASE_ANON_KEY=sua_anon_key_aqui

Crie um arquivo .env na raiz do projeto (se ainda não existir) e cole as variáveis.

Reinicie o servidor de desenvolvimento após salvar o .env.